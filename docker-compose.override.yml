version: '3.7'
services:
  chabok-service:
    build:
      context: ./backend/
    volumes:
      - ./backend/:/usr/src/app
      - ./backend/node_modules:/usr/src/app/node_modules
    command: npm run start:dev
networks:
  chabok:
    driver: bridge    